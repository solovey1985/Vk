<Page x:Class="VkGUI.View.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="LoginPage">

    <Grid> 
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Column="1" LastChildFill="False">
            <Label DockPanel.Dock="Top" Content="Авторизируйтесь, пожалуйста."></Label>
            <Label DockPanel.Dock="Top" Content="Ваш логин" ></Label>
            <TextBox DockPanel.Dock="Top" Margin="5 0 5 5" x:Name="txbxLogin" Text="{Binding Path=Login.Login, UpdateSourceTrigger=LostFocus}"></TextBox>
            <Label DockPanel.Dock="Top" Content="Ваш пароль"></Label>
            <TextBox DockPanel.Dock="Top" Margin="5 0 5 5" x:Name="txbxPass" Text="{Binding Path=Login.Pass, UpdateSourceTrigger=LostFocus}"></TextBox>
            <Button DockPanel.Dock="Top"  Content="Войти" Margin="5 10 5 5" Command="{Binding LoginCommand}"/>
            

        </DockPanel>
    </Grid>
</Page>
